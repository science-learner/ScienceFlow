# core | ml | gpu | mlebench | full
SCIENCEFLOW_TARGET=core

# Host directory mounted read/write at /workspace in the container.
SCIENCEFLOW_WORKSPACE=../workspace

# Host models.json mounted read-only into the container.
SCIENCEFLOW_MODEL_CONFIG=../models.json
