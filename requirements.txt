# Lightweight CPU-only ScienceFlow control-plane dependencies. Prefer ``uv sync`` for the
# locked environment; task/model stacks are available through project extras.

python-dotenv>=1.0
pydantic>=2.0
omegaconf>=2.3
pyyaml>=6.0
psutil>=5.9
aiofiles>=23.0
rich>=13.0
click>=8.1
dataclasses-json>=0.6.0
openai>=1.0
httpx>=0.27
tenacity>=9.0
lazy-loader>=0.4
inquirycraft[openai,tui]==0.9.0
backports-zstd>=1.3
tornado>=6.4
reportlab>=4.2,<5
